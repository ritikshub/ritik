# internal tooling — never publish with the site
.claude/
.impeccable.md
.DS_Store
.playwright-mcp/

# wrangler's local state — the worker source is the only thing worth keeping
worker/node_modules/
worker/.wrangler/
